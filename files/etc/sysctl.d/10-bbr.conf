# ==========================================
# 启用 BBR 与 TCP 优化 (保持 OpenWrt 默认 fq_codel)
# ==========================================

# 将 TCP 拥塞控制算法设置为 BBR
net.ipv4.tcp_congestion_control=bbr

# 开启 TCP Fast Open (减少握手延迟，对代理连接提升明显)
net.ipv4.tcp_fastopen=3
